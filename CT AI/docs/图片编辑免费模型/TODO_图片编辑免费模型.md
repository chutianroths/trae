# TODO_图片编辑免费模型

| 序号 | 待办 | 说明 |
| --- | --- | --- |
| T1 | 后端多模型调度 | 目前后端仍固定调用 Gemini，若要真正使用国内模型，需要扩展 POST /api/modules 以根据 selectedModel 路由请求，并处理不同供应商的签名/错误码。 |
| T2 | API Key 校验提示 | SettingsPanel 仅在前端存储密钥；可考虑添加“快速检测”按钮，调用各模型的 ping 接口以提示配额/鉴权是否有效。 |
| T3 | 配额监控 | 在 Sidebar 或状态栏展示国内模型剩余免费额度（通过供应商 API 或用户手动录入），减少超额风险。 |
| T4 | 更多国内模型 | 根据用户反馈补充如日日新·星辰/科大讯飞星火图像等模型，并复用 reeTier 字段。 |
